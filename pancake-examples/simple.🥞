// fun main() {
  //  var x = 0;
    // st8 @base, 0;
    // stw @base + 1, (42 << 16) | (4 << 8) | 1;
    // var x = < 10, 11 >;
    // !stw @base, x;
 //   return x;
//}

fun a({1, 11} queue) {
    var x = lds {1,    2} 0;
    var y = < 1, 100>;
    return 42;
}

fun main() {
    var item = lds {1} 0;
    a(item);
    return 0;
}